<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Public media
    |--------------------------------------------------------------------------
    |
    | Media files are served publicly via the /media/{path} endpoint.
    | URLs returned by the API are absolute and use the application's APP_URL.
    | No signing, secret, or TTL is currently applied.
    |
    */
];
