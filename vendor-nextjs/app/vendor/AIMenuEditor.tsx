import { useState } from 'react';
import { Sparkles, Languages, Tag, AlertCircle, Camera, Plus, X, TrendingUp } from 'lucide-react';
import { AIBadge, AITooltip, AISuggestionChip, AIGeneratedField, AIInsightCard, AIThinking } from '@/components/ai/AIComponents';
import { Input } from '@/components/ui/input';
import { toast } from 'sonner';

interface MenuItem {
  id: string;
  name: string;
  description: string;
  price: number;
  category: string;
  image?: string;
  allergens: string[];
  dietaryLabels: string[];
  spiceLevel: number;
  nutrition?: {
    calories: number;
    protein: number;
    carbs: number;
    fat: number;
  };
  translations?: {
    [lang: string]: {
      name: string;
      description: string;
    };
  };
  aiGenerated: {
    description: boolean;
    allergens: boolean;
    nutrition: boolean;
    translations: boolean;
  };
}

export function AIMenuEditor() {
  const [item, setItem] = useState<MenuItem>({
    id: 'item_001',
    name: 'Margherita Pizza',
    description: '',
    price: 12.90,
    category: 'Pizza',
    allergens: [],
    dietaryLabels: [],
    spiceLevel: 0,
    aiGenerated: {
      description: false,
      allergens: false,
      nutrition: false,
      translations: false
    }
  });

  const [isGenerating, setIsGenerating] = useState(false);
  const [showAIInsights, setShowAIInsights] = useState(true);

  // Simulate AI generation
  const generateDescription = async () => {
    setIsGenerating(true);
    await new Promise(resolve => setTimeout(resolve, 1500));
    
    setItem({
      ...item,
      description: 'Classic Italian pizza with fresh tomato sauce, creamy mozzarella, fragrant basil leaves, and extra virgin olive oil on a crispy thin crust.',
      aiGenerated: { ...item.aiGenerated, description: true }
    });
    
    setIsGenerating(false);
    toast.success('Description generated by AI');
  };

  const detectAllergens = async () => {
    setIsGenerating(true);
    await new Promise(resolve => setTimeout(resolve, 1000));
    
    setItem({
      ...item,
      allergens: ['Gluten', 'Dairy'],
      dietaryLabels: ['Vegetarian'],
      aiGenerated: { ...item.aiGenerated, allergens: true }
    });
    
    setIsGenerating(false);
    toast.success('Allergens detected by AI');
  };

  const estimateNutrition = async () => {
    setIsGenerating(true);
    await new Promise(resolve => setTimeout(resolve, 1200));
    
    setItem({
      ...item,
      nutrition: {
        calories: 850,
        protein: 32,
        carbs: 98,
        fat: 35
      },
      aiGenerated: { ...item.aiGenerated, nutrition: true }
    });
    
    setIsGenerating(false);
    toast.success('Nutrition estimated by AI');
  };

  const translateMenu = async () => {
    setIsGenerating(true);
    await new Promise(resolve => setTimeout(resolve, 2000));
    
    setItem({
      ...item,
      translations: {
        de: {
          name: 'Margherita Pizza',
          description: 'Klassische italienische Pizza mit frischer Tomatensauce, cremigem Mozzarella, duftendem Basilikum und nativem Olivenöl extra auf knusprigem dünnem Boden.'
        },
        fr: {
          name: 'Pizza Margherita',
          description: 'Pizza italienne classique avec sauce tomate fraîche, mozzarella crémeuse, feuilles de basilic parfumées et huile d\'olive extra vierge sur pâte fine croustillante.'
        },
        es: {
          name: 'Pizza Margarita',
          description: 'Pizza italiana clásica con salsa de tomate fresca, mozzarella cremosa, hojas de albahaca aromáticas y aceite de oliva virgen extra sobre masa fina crujiente.'
        }
      },
      aiGenerated: { ...item.aiGenerated, translations: true }
    });
    
    setIsGenerating(false);
    toast.success('Menu translated to 3 languages');
  };

  const applyPriceSuggestion = () => {
    setItem({ ...item, price: 13.50 });
    toast.success('Price updated to €13.50');
  };

  return (
    <div className="p-6 max-w-5xl mx-auto">
      {/* Header */}
      <div className="mb-6">
        <h1 className="text-2xl mb-1">Edit Menu Item</h1>
        <p className="text-sm text-gray-500">AI can help you create professional menu content</p>
      </div>

      {/* AI Insights */}
      {showAIInsights && (
        <div className="mb-6 space-y-3">
          <AIInsightCard
            type="recommendation"
            title="Pricing Opportunity"
            description="This dish is frequently viewed but rarely ordered. Based on similar items, you could increase the price to €13.50 without reducing orders."
            metric="Current conversion: 12% • Expected after change: 11-13%"
            action={{
              label: 'Apply suggestion',
              onClick: applyPriceSuggestion
            }}
            explanation="Based on 847 views and 102 orders over the last 30 days, compared with 23 similar dishes in your area"
            onDismiss={() => setShowAIInsights(false)}
          />
        </div>
      )}

      <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
        {/* Main Content */}
        <div className="lg:col-span-2 space-y-6">
          {/* Basic Info */}
          <div className="bg-white rounded-xl border border-gray-200 p-6">
            <h2 className="text-lg mb-4">Basic Information</h2>
            <div className="space-y-4">
              <div>
                <label className="block text-sm font-medium mb-2">Item Name</label>
                <Input
                  value={item.name}
                  onChange={(e) => setItem({ ...item, name: e.target.value })}
                />
              </div>

              <div>
                <div className="flex items-center justify-between mb-2">
                  <label className="text-sm font-medium">Description</label>
                  {!item.description && !isGenerating && (
                    <button
                      onClick={generateDescription}
                      className="flex items-center gap-1.5 px-3 py-1.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 text-sm"
                    >
                      <Sparkles className="w-4 h-4" />
                      Generate with AI
                    </button>
                  )}
                </div>
                {isGenerating && !item.description ? (
                  <div className="p-4 bg-purple-50 rounded-lg">
                    <AIThinking message="Creating description..." />
                  </div>
                ) : item.aiGenerated.description ? (
                  <AIGeneratedField
                    label=""
                    value={item.description}
                    onChange={(val) => setItem({ ...item, description: val })}
                    onRegenerate={generateDescription}
                    confidence={92}
                    multiline
                  />
                ) : (
                  <textarea
                    value={item.description}
                    onChange={(e) => setItem({ ...item, description: e.target.value })}
                    className="w-full p-3 border border-gray-200 rounded-lg text-sm resize-none"
                    rows={4}
                    placeholder="Enter a description or let AI generate one"
                  />
                )}
              </div>

              <div className="grid grid-cols-2 gap-4">
                <div>
                  <label className="block text-sm font-medium mb-2">Price (€)</label>
                  <Input
                    type="number"
                    step="0.10"
                    value={item.price}
                    onChange={(e) => setItem({ ...item, price: parseFloat(e.target.value) })}
                  />
                </div>
                <div>
                  <label className="block text-sm font-medium mb-2">Category</label>
                  <select 
                    className="w-full px-3 py-2 border border-gray-200 rounded-lg"
                    value={item.category}
                    onChange={(e) => setItem({ ...item, category: e.target.value })}
                  >
                    <option>Pizza</option>
                    <option>Pasta</option>
                    <option>Salad</option>
                    <option>Appetizers</option>
                    <option>Desserts</option>
                  </select>
                </div>
              </div>
            </div>
          </div>

          {/* Allergens & Dietary */}
          <div className="bg-white rounded-xl border border-gray-200 p-6">
            <div className="flex items-center justify-between mb-4">
              <h2 className="text-lg">Allergens & Dietary Info</h2>
              {item.allergens.length === 0 && !isGenerating && (
                <button
                  onClick={detectAllergens}
                  className="flex items-center gap-1.5 px-3 py-1.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 text-sm"
                >
                  <Sparkles className="w-4 h-4" />
                  Auto-detect
                </button>
              )}
            </div>

            {isGenerating && item.allergens.length === 0 ? (
              <div className="p-4 bg-purple-50 rounded-lg">
                <AIThinking message="Detecting allergens..." />
              </div>
            ) : (
              <div className="space-y-4">
                <div>
                  <div className="flex items-center gap-2 mb-2">
                    <label className="text-sm font-medium">Allergens</label>
                    {item.aiGenerated.allergens && <AIBadge />}
                  </div>
                  <div className="flex flex-wrap gap-2">
                    {item.allergens.map((allergen) => (
                      <span 
                        key={allergen}
                        className="px-3 py-1.5 bg-red-100 text-red-700 rounded-full text-sm flex items-center gap-1.5"
                      >
                        {allergen}
                        <button
                          onClick={() => setItem({
                            ...item,
                            allergens: item.allergens.filter(a => a !== allergen)
                          })}
                          className="hover:bg-red-200 rounded-full"
                        >
                          <X className="w-3 h-3" />
                        </button>
                      </span>
                    ))}
                    {item.allergens.length === 0 && (
                      <span className="text-sm text-gray-500">No allergens detected</span>
                    )}
                  </div>
                </div>

                <div>
                  <div className="flex items-center gap-2 mb-2">
                    <label className="text-sm font-medium">Dietary Labels</label>
                    {item.aiGenerated.allergens && <AIBadge />}
                  </div>
                  <div className="flex flex-wrap gap-2">
                    {item.dietaryLabels.map((label) => (
                      <span 
                        key={label}
                        className="px-3 py-1.5 bg-green-100 text-green-700 rounded-full text-sm flex items-center gap-1.5"
                      >
                        {label}
                        <button
                          onClick={() => setItem({
                            ...item,
                            dietaryLabels: item.dietaryLabels.filter(l => l !== label)
                          })}
                          className="hover:bg-green-200 rounded-full"
                        >
                          <X className="w-3 h-3" />
                        </button>
                      </span>
                    ))}
                    {item.dietaryLabels.length === 0 && (
                      <span className="text-sm text-gray-500">No dietary labels</span>
                    )}
                  </div>
                </div>
              </div>
            )}
          </div>

          {/* Nutrition */}
          <div className="bg-white rounded-xl border border-gray-200 p-6">
            <div className="flex items-center justify-between mb-4">
              <h2 className="text-lg">Nutritional Information</h2>
              {!item.nutrition && !isGenerating && (
                <button
                  onClick={estimateNutrition}
                  className="flex items-center gap-1.5 px-3 py-1.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 text-sm"
                >
                  <Sparkles className="w-4 h-4" />
                  Estimate with AI
                </button>
              )}
            </div>

            {isGenerating && !item.nutrition ? (
              <div className="p-4 bg-purple-50 rounded-lg">
                <AIThinking message="Estimating nutrition..." />
              </div>
            ) : item.nutrition ? (
              <div>
                {item.aiGenerated.nutrition && (
                  <div className="flex items-center gap-2 mb-3 text-sm text-purple-700">
                    <AIBadge />
                    <span>Estimated values - you can adjust</span>
                    <AITooltip
                      title="How we estimate"
                      explanation="Based on similar dishes and standard ingredient databases"
                      confidence={85}
                    />
                  </div>
                )}
                <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
                  <div>
                    <label className="block text-xs text-gray-500 mb-1">Calories</label>
                    <Input
                      type="number"
                      value={item.nutrition.calories}
                      onChange={(e) => setItem({
                        ...item,
                        nutrition: { ...item.nutrition!, calories: parseInt(e.target.value) }
                      })}
                    />
                  </div>
                  <div>
                    <label className="block text-xs text-gray-500 mb-1">Protein (g)</label>
                    <Input
                      type="number"
                      value={item.nutrition.protein}
                      onChange={(e) => setItem({
                        ...item,
                        nutrition: { ...item.nutrition!, protein: parseInt(e.target.value) }
                      })}
                    />
                  </div>
                  <div>
                    <label className="block text-xs text-gray-500 mb-1">Carbs (g)</label>
                    <Input
                      type="number"
                      value={item.nutrition.carbs}
                      onChange={(e) => setItem({
                        ...item,
                        nutrition: { ...item.nutrition!, carbs: parseInt(e.target.value) }
                      })}
                    />
                  </div>
                  <div>
                    <label className="block text-xs text-gray-500 mb-1">Fat (g)</label>
                    <Input
                      type="number"
                      value={item.nutrition.fat}
                      onChange={(e) => setItem({
                        ...item,
                        nutrition: { ...item.nutrition!, fat: parseInt(e.target.value) }
                      })}
                    />
                  </div>
                </div>
              </div>
            ) : (
              <p className="text-sm text-gray-500">No nutritional information added</p>
            )}
          </div>

          {/* Translations */}
          <div className="bg-white rounded-xl border border-gray-200 p-6">
            <div className="flex items-center justify-between mb-4">
              <h2 className="text-lg">Multi-language Support</h2>
              {!item.translations && item.description && !isGenerating && (
                <button
                  onClick={translateMenu}
                  className="flex items-center gap-1.5 px-3 py-1.5 bg-purple-600 text-white rounded-lg hover:bg-purple-700 text-sm"
                >
                  <Languages className="w-4 h-4" />
                  Translate with AI
                </button>
              )}
            </div>

            {isGenerating && !item.translations ? (
              <div className="p-4 bg-purple-50 rounded-lg">
                <AIThinking message="Translating to 3 languages..." />
              </div>
            ) : item.translations ? (
              <div className="space-y-4">
                {item.aiGenerated.translations && (
                  <div className="flex items-center gap-2 mb-2 text-sm text-purple-700">
                    <AIBadge />
                    <span>AI-generated translations</span>
                    <AITooltip
                      title="Translation quality"
                      explanation="Translations are contextually adapted for restaurant menus"
                      confidence={94}
                    />
                  </div>
                )}
                {Object.entries(item.translations).map(([lang, content]) => (
                  <div key={lang} className="p-4 bg-gray-50 rounded-lg">
                    <div className="flex items-center gap-2 mb-2">
                      <span className="text-sm font-medium">
                        {lang === 'de' && '🇩🇪 German'}
                        {lang === 'fr' && '🇫🇷 French'}
                        {lang === 'es' && '🇪🇸 Spanish'}
                      </span>
                    </div>
                    <div className="text-sm font-medium mb-1">{content.name}</div>
                    <div className="text-sm text-gray-700">{content.description}</div>
                  </div>
                ))}
              </div>
            ) : (
              <p className="text-sm text-gray-500">
                {item.description 
                  ? 'Click "Translate with AI" to add multi-language support'
                  : 'Add a description first to enable translation'}
              </p>
            )}
          </div>
        </div>

        {/* Sidebar - AI Suggestions */}
        <div className="space-y-4">
          <div className="bg-purple-50 rounded-xl border border-purple-200 p-4">
            <div className="flex items-center gap-2 mb-3">
              <Sparkles className="w-5 h-5 text-purple-600" />
              <h3 className="font-medium text-purple-900">AI Suggestions</h3>
            </div>
            <div className="space-y-2 text-sm text-purple-800">
              <p>AI can help you:</p>
              <ul className="space-y-1.5 ml-4">
                <li>• Generate professional descriptions</li>
                <li>• Detect allergens automatically</li>
                <li>• Estimate nutritional values</li>
                <li>• Translate to 11 languages</li>
                <li>• Optimize pricing</li>
              </ul>
            </div>
          </div>

          <div className="bg-white rounded-xl border border-gray-200 p-4">
            <h3 className="font-medium mb-3">Quick Actions</h3>
            <div className="space-y-2">
              <button className="w-full py-2 bg-purple-600 text-white rounded-lg hover:bg-purple-700 text-sm">
                Save Changes
              </button>
              <button className="w-full py-2 border border-gray-300 rounded-lg hover:bg-gray-50 text-sm">
                Preview
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
